apiVersion: v1
kind: ConfigMap
metadata:
  name: acme-monitoring-config
  namespace: acme-monitoring
  annotations:
    ran.openshift.io/ztp-deploy-wave: "10"
data:
  monitoring.conf: |
    interval=30
    endpoint=https://monitor.acme.example.com
